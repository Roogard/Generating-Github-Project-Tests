services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ggpt_data:/data
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:////data/ggpt.db
      - CHROMA_PATH=/data/chroma_db

volumes:
  ggpt_data:
